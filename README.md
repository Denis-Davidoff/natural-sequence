## Find skipped natural numbers

Install packages by using **npm install** or **yarn install**

Compile and run script
```
npm run start
```

Only compile script
```
npm run compile
```

Run tests. Compile or run script first
```
npm run test
```